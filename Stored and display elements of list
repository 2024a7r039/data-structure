//Write a program to store N elements into the list(Array) and display them.
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    
    int elements[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", &elements[i]);
    }
    
    for (int i = 0; i < n; i++) {
        printf("%d ", elements[i]);
    }
    return 0;
}
